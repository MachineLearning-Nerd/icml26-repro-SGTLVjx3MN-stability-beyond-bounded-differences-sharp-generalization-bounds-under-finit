# Claim 5 limitations and deviations

The FALSIFIED verdict is scoped specifically to the exact displayed Theorem
3.10 and therefore to the combined Claim 5. It does not assert that Theorem
3.9 is false. It also does not assert that no corrected transductive
generalization theorem is possible.

The standard zero-variance convention is used:
`exp(-c y^2/0)=0` for `y>0`, the continuous limit required by any concentration
bound. The counterexample is exhaustive, deterministic conditional on each
partition, and needs no statistical uncertainty estimate.
