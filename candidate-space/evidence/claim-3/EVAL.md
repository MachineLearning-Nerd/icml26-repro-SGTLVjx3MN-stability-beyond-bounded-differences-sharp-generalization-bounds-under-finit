# Claim 3 — VERIFIED

The Gaussian sample-mean witness directly satisfies every quantifier in
Assumption 3.1 at `p=2`, `beta=1/m`, and `H(z,z')=|z-z'|`. Its exact envelope
moment is two. A quantified witness family proves that no finite deterministic
uniform-stability constant exists, so the example establishes the claimed
strict extension rather than merely observing a finite sample moment.

Release readiness remains conditional on the fixed OpenResearch run, its
independent-checker output, and evaluator-visible mirroring.
