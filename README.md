# Historical rejected baseline

This root experiment freezes the toy-scale state judged **6/12** for
“Stability beyond Bounded Differences: Sharp Generalization Bounds under
Finite \(L_p\) Moments” (arXiv:2606.06855).

The six checks are intentionally labeled as proxies. They do not verify the
paper’s universal theorems. Current theorem-level work belongs on child
experiments and must supersede this page without rewriting this root.

Run the frozen command:

```bash
uv run --locked python -m reproduction.run_all
```
